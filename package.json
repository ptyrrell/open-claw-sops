{
  "name": "open-claw-sops",
  "version": "0.2.0",
  "description": "Open Claw SOP playbook — markdown sources, HTML build, GitHub Pages deploy. SOPs created by Alfred from calls (Loom / Fathom / Zoom / Grain) or by hand.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "lint": "node scripts/lint-frontmatter.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^14.1.4"
  }
}
